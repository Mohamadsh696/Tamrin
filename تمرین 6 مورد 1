#include <stdio.h>

int main() {
  int n, max = -1;

  printf("Enter numbers (0 to quit):\n");

  while (1) {
    scanf("%d", &n);

    if (n == 0) {
      break;
    }

    if (n > max) {
      max = n;
    }
  }

  printf("The maximum number is %d\n", max);

  return 0;
}
