#include <stdio.h>

int main() {

  int num1, num2;

  printf("دو عدد را وارد کنید: ");
  scanf("%d %d", &num1, &num2);

  if (num1 > num2) {
    int temp = num1;
    num1 = num2;
    num2 = temp;
  }

  for (int i = num1 + 1; i <= num2; i += 2) {
    printf("%d\n", i);
  }

  return 0;
}
